import React from 'react';
import '../styles/loading.css';  // Assuming you save the CSS in a file named Loader.css

const Loader = () => {
    return (
        <div className="loader">
            Loading
            <span></span>
        </div>
    );
};

export default Loader;
